import { Router } from 'express';

const router = Router();

// Mount v1 API route groups here:
// import authRouter from './auth.route.js';
// router.use('/auth', authRouter);

export default router;
